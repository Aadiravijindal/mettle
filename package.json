{
  "name": "mettle",
  "private": true,
  "version": "0.1.0",
  "description": "AI-fluency hiring assessment MVP: record a candidate's real work session, analyze it with Claude, report to the founder.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
